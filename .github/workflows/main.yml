name: Build TESTPLUGIN (.NET 4.8 - RevitLookup Style)
on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: 🧩 Setup MSBuild
        uses: microsoft/setup-msbuild@v2

      - name: 📦 Download Revit API DLLs
        shell: pwsh
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release download v1.0.0 --repo SHarpreet89/MyRevitAddin --pattern RevitApis.zip --dir .
          Expand-Archive RevitApis.zip -DestinationPath revapi -Force
          Copy-Item (Get-ChildItem -Recurse revapi -Filter 'RevitAPI.dll' | Select -First 1).FullName RevitAPI.dll -Force
          Copy-Item (Get-ChildItem -Recurse revapi -Filter 'RevitAPIUI.dll' | Select -First 1).FullName RevitAPIUI.dll -Force

      - name: 🏗️ MSBuild TESTPLUGIN x64
        run: |
          msbuild TESTPLUGIN.csproj /p:Configuration=Release /p:Platform=x64 /t:Build /p:OutputPath=bin\x64\Release\ /maxcpucount /v:minimal

      - name: 🔍 Verify
        shell: pwsh
        run: |
          $dll = "bin\x64\Release\TESTPLUGIN.dll"
          Get-ChildItem -Recurse -Filter "TESTPLUGIN.dll"
          & "C:\Program Files (x86)\Microsoft SDKs\Windows\v10.0A\bin\NETFX 4.8 Tools\x64\corflags.exe" $dll

      - name: 📤 Upload
        uses: actions/upload-artifact@v4
        with:
          name: TESTPLUGIN-REVIT2025-FINAL
          path: bin/x64/Release/TESTPLUGIN.dll
          retention-days: 90
