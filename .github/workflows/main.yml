name: Build Revit Addin (.NET 8)

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: 🧾 Checkout repository
        uses: actions/checkout@v4

      - name: 🧩 Setup .NET 8 SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      # 🧠 Verify .NET Windows Desktop runtime
      - name: 🧠 Verify .NET Windows Desktop runtime
        shell: pwsh
        run: |
          $runtimes = dotnet --list-runtimes
          Write-Host "Installed runtimes:`n$runtimes"
      
          # Normalize whitespace (in case everything is printed on one line)
          $normalized = ($runtimes -replace '\s+', ' ')
      
          if ($normalized -match 'Microsoft\.WindowsDesktop\.App\s*8\.0') {
            Write-Host "✅ WindowsDesktop.App 8.0 runtime detected."
          } else {
            throw "❌ Missing WindowsDesktop.App — Revit add-ins need net8.0-windows SDK!"
          }

      # 📦 Download Revit API DLLs via gh (robust method)
      - name: 📦 Download Revit API DLLs via gh
        shell: pwsh
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          $ErrorActionPreference = 'Stop'
          $repo  = 'SHarpreet89/MyRevitAddin'
          $tag   = 'v1.0.0'
          $asset = 'RevitApis.zip'

          Write-Host "📥 Downloading $asset from $repo@$tag ..."
          gh release download $tag --repo $repo --pattern $asset --dir .

          if (-not (Test-Path $asset)) { throw "❌ $asset was not downloaded." }

          $extractDir = Join-Path $PWD 'revapi'
          if (Test-Path $extractDir) { Remove-Item $extractDir -Recurse -Force }
          New-Item -ItemType Directory -Path $extractDir | Out-Null

          Expand-Archive -Path $asset -DestinationPath $extractDir -Force

          # Find both DLLs
          $api   = Get-ChildItem -Recurse $extractDir -Filter 'RevitAPI.dll'    | Select-Object -First 1
          $apiUI = Get-ChildItem -Recurse $extractDir -Filter 'RevitAPIUI.dll' | Select-Object -First 1
          if (-not $api -or -not $apiUI) {
            Get-ChildItem -Recurse $extractDir | ForEach-Object { Write-Host $_.FullName }
            throw "❌ RevitAPI.dll and/or RevitAPIUI.dll not found in ZIP."
          }

          # ✅ Validate FILE VERSION (not size). Expect 25.* for Revit 2025.
          function Get-Ver($p) { ([System.Diagnostics.FileVersionInfo]::GetVersionInfo($p)).FileVersion }
          $verApi   = Get-Ver $api.FullName
          $verApiUI = Get-Ver $apiUI.FullName
          Write-Host "🔎 Versions -> RevitAPI: $verApi | RevitAPIUI: $verApiUI"

          if ($verApi   -notmatch '^25\.' ) { throw "❌ RevitAPI.dll version ($verApi) is not 25.* (wrong year or corrupted)." }
          if ($verApiUI -notmatch '^25\.' ) { throw "❌ RevitAPIUI.dll version ($verApiUI) is not 25.* (wrong year or corrupted)." }

          # Stage them to repo root for HintPath
          Copy-Item $api.FullName   -Destination (Join-Path $PWD 'RevitAPI.dll')   -Force
          Copy-Item $apiUI.FullName -Destination (Join-Path $PWD 'RevitAPIUI.dll') -Force

          Write-Host "✅ Staged RevitAPI DLLs in repo root."

      - name: 🏗️ Build (Release, net8.0-windows, x64)
        run: dotnet build MyRevitAddin.csproj --configuration Release

      - name: 📤 Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: MyRevitAddin
          path: bin/Release/net8.0-windows/
